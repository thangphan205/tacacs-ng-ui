"""Read-only MCP (Model Context Protocol) server for TACACS+ config generation.

Named `mcp_server` rather than `mcp` so that `from mcp.server.fastmcp import ...`
inside this package unambiguously refers to the SDK.
"""
